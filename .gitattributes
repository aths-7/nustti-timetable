# 统一按 LF 入库，避免跨平台换行符差异
* text=auto eol=lf

# Windows 批处理必须保持 CRLF
*.bat text eol=crlf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.ttf binary
*.ttc binary
*.jar binary
*.war binary
*.apk binary
