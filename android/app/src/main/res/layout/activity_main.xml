<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <LinearLayout
        android:id="@+id/contentHost"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- 顶部液态玻璃标题栏：标题文字 + 右上三点独立小玻璃容器（自绘玻璃，主题色氛围层） -->
        <edu.nustti.timetable.ui.GlassToolbarView
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize" />

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/viewPager"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />
    </LinearLayout>

    <!-- 苹果 Dock 样式液态玻璃导航栏：悬浮内容之上、水平居中、左右留边距。
         左右留边由 DockBarView 内部子层（模糊层全宽铺满、雾面 mask 与图标行 margin 32dp）实现，
         不在根布局留 padding，避免 padding 区露出模糊层矩形边缘导致左右露白 -->
    <edu.nustti.timetable.ui.DockBarView
        android:id="@+id/dockBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:paddingBottom="30dp" />
</FrameLayout>
